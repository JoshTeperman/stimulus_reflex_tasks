<h2 class="my-3"><%= @task.name %></h2>
<% if @task.assignee %>
  <p class="text-muted">Assigned to: <%= @task.assignee_name %></p>
<% end %>
<% if @task.completed? %>
  <p class="text-muted">Completed_by: <%= @task.completer_name %></p>
<% end %>

<%= link_to 'Back to lists', root_path %>

<h4 class="mb-3">Comments</h4>
<div id="comments">
  <%= render @task.comments.order(created_at: :asc) %>
</div>

<%= render 'comments/create_form', new_comment: @new_comment, task: @task %>
